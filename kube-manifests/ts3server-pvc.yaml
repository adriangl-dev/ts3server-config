apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ts3server-pvc
  namespace: ts3server
  labels:
    app: ts3server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi